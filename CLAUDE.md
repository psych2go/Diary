# CLAUDE.md

## 项目说明

自托管日记应用。日记可以通过手机输入法语音转文字，再由网页服务直接记录到
Markdown 文件中。应用不依赖 GitHub 托管，也不会自动 commit 或 push。

正式部署使用 Cloudflare R2 保存 Restic 加密备份。云端不保存可直接阅读的 Markdown，
备份密钥不得写入仓库。

Android 客户端配置位于 `android/`。生产域名、包名和签名信息必须在发布前核对；
签名密钥只能保存在仓库之外的安全位置。

## 日记格式规范

- **文件路径**：`{YYYY}/{YYYYMMDD}.md`
- **文件标题**：第一行为 `# YYYY-MM-DD`
- **时间戳**：每次用户口述一段内容，必须获取当前时间并以 `## HH:MM` 作为段落标题，写在内容之前
- **段落之间**：空一行分隔

## 写日记流程

当用户要求记录日记时，按以下步骤操作：

1. **创建文件**（如当天文件不存在）：
   - 确认年份目录存在，不存在则创建
   - 创建当天文件，写入标题和时间戳
2. **追加内容**：
   - 获取当前时间，生成时间戳
   - 将用户口述的文字追加到文件末尾
   - 口述内容保持原样，不做改写或润色
3. **修改内容**：
   - 用户要求修改时，精确定位并修改指定内容
4. **备份**：
   - 网页服务只负责写入本地 Markdown
   - 不自动执行 Git 操作
   - 如需 Git、对象存储或服务器快照备份，按用户明确要求执行

## 注意事项

- 口述文字中的专有名词用户可能会纠正拼写，按用户要求修改即可
- 不主动修改用户的语气、用词或表达方式
- 日记内容不得提交到源码仓库，只执行用户明确要求的记录、修改和备份操作
